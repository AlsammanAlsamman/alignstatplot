% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseTreeHeatmap.R
\name{baseTreeHeatmap}
\alias{baseTreeHeatmap}
\title{Draw a phylogenetic tree next to a heatmap of a matrix, base graphics}
\usage{
baseTreeHeatmap(
  tree,
  X,
  fsize = c(1, 1, 1),
  colors = NULL,
  standardize = FALSE,
  legend = TRUE
)
}
\arguments{
\item{tree}{an object of class "phylo"}

\item{X}{a numeric matrix whose rownames match \code{tree$tip.label}}

\item{fsize}{length-3 vector: tip label size, column label size, legend size
(matches \code{phylo.heatmap(fsize=)}); a single value is recycled to all three}

\item{colors}{a vector of colors for the heatmap gradient}

\item{standardize}{logical, z-score standardize each column before plotting}

\item{legend}{logical, draw a color-scale legend}
}
\value{
invisible NULL (side effect only)
}
\description{
Internal helper replacing \code{phytools::phylo.heatmap()}.
Draws the tree with \code{ape::plot.phylo} (already used elsewhere in this
package, e.g. \code{plotTreeWithRuler}) and the matrix with base
\code{graphics::image()}, laid out side by side via \code{graphics::layout()}.
Like \code{phylo.heatmap()}, this is a base-graphics side effect (draws to
the active device) rather than a returned plot object - matching current
behavior exactly, since \code{phylo.heatmap()} itself never returned a
reusable object either.
}
