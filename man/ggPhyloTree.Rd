% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggPhyloTree.R
\name{ggPhyloTree}
\alias{ggPhyloTree}
\title{Build a rectangular phylogenetic tree as a ggplot object}
\usage{
ggPhyloTree(tree, tip_size = 3, align = TRUE, expand = 0.4)
}
\arguments{
\item{tree}{an object of class "phylo"}

\item{tip_size}{tip label font size (matches \code{geom_tiplab(size=)})}

\item{align}{logical, right-align tip labels with a dotted leader line
(matches \code{geom_tiplab(align=TRUE)})}

\item{expand}{extra fractional space to the right of the plot for label text
(matches \code{hexpand(.4)})}
}
\value{
a list with `plot` (a ggplot object) and `tip_order` (tip labels ordered
top-to-bottom as rendered, for reordering companion row-aligned plots)
}
\description{
Internal helper replacing \code{ggtree(tree) + geom_tiplab(align=TRUE) +
hexpand(.4)}. Uses only \code{ape}'s own tree-layout coordinates (via the documented
\code{ape:::.PlotPhyloEnv} pattern also used by \code{phytools} and other ape-ecosystem
packages) plus \code{ggplot2} primitives, so the result stays a plain \code{ggplot} object
that composes with \code{cowplot::plot_grid()} exactly like the ggtree version did - with
no Bioconductor dependency.
}
