#Default fill palette for nucleotide heatmaps/frequency plots (A, T, C, G, N).
#Shared by nucTableHeatmap() and nucFrequencyPlot() so the two stay in sync
#instead of each hardcoding its own copy of the same literal color vector.
defaultNucleotideColors<-c(A = "red", T = "blue", C = "green", G = "yellow", N = "black")
